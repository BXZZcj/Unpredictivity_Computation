# 1. Virtual Environments
# Never commit your virtual environment files.
# They are specific to your machine and OS.
# Your dependencies should be managed by a `requirements.txt` or `pyproject.toml` file.
venv/
.venv/
env/
.env/
ENV/
sh-activate
.tox/
.nox/
.conda/

# 2. Python Bytecode and Caches
# These are auto-generated and will be recreated as needed.
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version

# 3. Distribution / Packaging / Build artifacts
# These are generated when you build your package for distribution.
build/
dist/
downloads/
eggs/
*.egg-info/
sdist/
wheels/
pip-wheel-metadata/
*.egg
*.whl
MANIFEST

# 4. IDEs and Editors Configuration
# Each developer has their own setup.
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace
.project
.pydevproject
.settings/

# 5. Secrets and Local Configuration
# CRITICAL: Never commit files containing sensitive data like API keys or passwords!
# Instead, create a `example.env` or similar template file and add it to git.
.env
.env.*
.secrets/
*.local
secrets.yml
config.ini  # Be careful with this one if it contains secrets

# 6. Testing & Coverage Reports
# These are generated each time you run your tests.
.pytest_cache/
.coverage
.coverage.*
htmlcov/
nosetests.xml
coverage.xml
pytest-report.html
.hypothesis/

# 7. Logs and Databases
# Log files and local databases should not be version controlled.
*.log
*.db
*.sqlite3
*.rdb
# Be more specific if you need to track a certain type of DB file, e.g., !my_project/data.db

# 8. Operating System specific files
# Annoying files generated by different OS.
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# 9. Jupyter Notebook Checkpoints
# Auto-saves from Jupyter can be ignored.
.ipynb_checkpoints/

# 10. Type Checking Cache
# Cache files from tools like MyPy.
.mypy_cache/
.dmypy.json
dmypy.json

# 11. Custom Ignore Patterns
ESOT500-L/